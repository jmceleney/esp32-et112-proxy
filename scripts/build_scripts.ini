; Additional build script settings

[extra]
; Pre-build script to extract git information and make it available to the build
; Use absolute path for better compatibility with VSCode
extra_scripts = pre:${PROJECT_DIR}/scripts/pre_build.py 